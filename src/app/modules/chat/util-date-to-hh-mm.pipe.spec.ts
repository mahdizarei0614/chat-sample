import { UtilDateToHhMmPipe } from './util-date-to-hh-mm.pipe';

describe('UtilDateToHhMmPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilDateToHhMmPipe();
    expect(pipe).toBeTruthy();
  });
});
