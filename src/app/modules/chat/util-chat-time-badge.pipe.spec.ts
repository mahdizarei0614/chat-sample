import { UtilChatTimeBadgePipe } from './util-chat-time-badge.pipe';

describe('UtilChatTimeBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new UtilChatTimeBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
